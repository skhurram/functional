(ns functional.component.persistence-test
  (:require [clojure.test :refer :all]
            [functional.component.persistence :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
